import { SettingsTabProvider } from 'tabby-settings';
export declare class SSHConfigImportSettingsTabProvider extends SettingsTabProvider {
    id: string;
    icon: string;
    title: string;
    getComponentType(): any;
}
